spring:
  datasource:
    url: jdbc:mysql://localhost:13306/my_rds_name?useSSL=false&serverTimezone=UTC
    username: admin
    password: kang020511
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    open-in-view: false